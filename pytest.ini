[pytest]
asyncio_mode = auto
testpaths = tests
norecursedirs = .git
log_format = %(asctime)s.%(msecs)03d %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
markers =
    enable_custom_integrations: mark test to enable custom integrations
    asyncio: mark test as asyncio-based
